#include <memory>
#include <vector>
#include <cmath>

class Point {
    public:
        float x, y, r, theta_in_radian; // Unit for r doesnt matter
        Point();
        Point( float X, float Y );

};